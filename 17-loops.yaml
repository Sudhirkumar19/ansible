# to install packages

- name: loops demo
  hosts: web
  become: yes
  tasks: 
  - name: install packages
    ansible.builtin.package: 
        msg: "{{item}}"
        state: present
    loop:
    - mysql
    - nginx
    - postfix
    - postgresql