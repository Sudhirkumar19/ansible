# to install packages

- name: loops demo
  hosts: web
  become: yes
  tasks: 
  - name: print names
    ansible.builtin.package: 
        msg: "Hello {{item}}"
        state: present
    loop:
    - mysql
    - nginx
    - postfix
    - postgresql